<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://pmaher1.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://pmaher1.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-07-02T08:49:23+00:00</updated><id>https://pmaher1.github.io/feed.xml</id><title type="html">blank</title><subtitle>Personal website for Patrick Maher, a research assistant in statistics at the University of Queensland. </subtitle><entry><title type="html">WIP</title><link href="https://pmaher1.github.io/blog/2025/wip/" rel="alternate" type="text/html" title="WIP"/><published>2025-09-25T00:00:00+00:00</published><updated>2025-09-25T00:00:00+00:00</updated><id>https://pmaher1.github.io/blog/2025/wip</id><content type="html" xml:base="https://pmaher1.github.io/blog/2025/wip/"><![CDATA[<p>WIP</p>]]></content><author><name></name></author><summary type="html"><![CDATA[WIP]]></summary></entry><entry><title type="html">Dose Finding Hierarchical Emax</title><link href="https://pmaher1.github.io/blog/2025/dose-finding-hierarchical-emax/" rel="alternate" type="text/html" title="Dose Finding Hierarchical Emax"/><published>2025-09-24T00:00:00+00:00</published><updated>2025-09-24T00:00:00+00:00</updated><id>https://pmaher1.github.io/blog/2025/dose-finding-hierarchical-emax</id><content type="html" xml:base="https://pmaher1.github.io/blog/2025/dose-finding-hierarchical-emax/"><![CDATA[<!DOCTYPE html> <html xmlns="http://www.w3.org/1999/xhtml" lang="en" xml:lang="en"><head> <meta charset="utf-8"> <meta name="generator" content="quarto-1.9.38"> <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=yes"> <meta name="dcterms.date" content="2025-09-24"> <meta name="description" content="Extending a Bayesian hierarchical EMAX model with expert-informed priors for a small dose-finding trial in type 1 diabetes, and testing it by simulation against independent pairwise comparisons."> <title>Dose-finding with a Bayesian hierarchical EMAX model</title> <style>code{white-space:pre-wrap}span.smallcaps{font-variant:small-caps}div.columns{display:flex;gap:min(4vw,1.5em)}div.column{flex:auto;overflow-x:auto}div.hanging-indent{margin-left:1.5em;text-indent:-1.5em}ul.task-list{list-style:none}ul.task-list li input[type="checkbox"]{width:.8em;margin:0 .8em .2em -1em;vertical-align:middle}html{-webkit-text-size-adjust:100%}pre>code.sourceCode{white-space:pre;position:relative}pre>code.sourceCode>span{display:inline-block;line-height:1.25}pre>code.sourceCode>span:empty{height:1.2em}.sourceCode{overflow:visible}code.sourceCode>span{color:inherit;text-decoration:inherit}div.sourceCode{margin:1em 0}pre.sourceCode{margin:0}@media screen{div.sourceCode{overflow:auto}}@media print{pre>code.sourceCode{white-space:pre-wrap}pre>code.sourceCode>span{text-indent:-5em;padding-left:5em}}pre.numberSource code{counter-reset:source-line 0}pre.numberSource code>span{position:relative;left:-4em;counter-increment:source-line}pre.numberSource code>span>a:first-child::before{content:counter(source-line);position:relative;left:-1em;text-align:right;vertical-align:baseline;border:0;display:inline-block;-webkit-touch-callout:none;-webkit-user-select:none;-khtml-user-select:none;-moz-user-select:none;-ms-user-select:none;user-select:none;padding:0 4px;width:4em}pre.numberSource{margin-left:3em;padding-left:4px}@media screen{pre>code.sourceCode>span>a:first-child::before{text-decoration:underline}}</style> <script src="2025-09-24-dose-finding-hierarchical-emax_files/libs/clipboard/clipboard.min.js"></script> <script src="2025-09-24-dose-finding-hierarchical-emax_files/libs/quarto-html/quarto.js" type="module"></script> <script src="2025-09-24-dose-finding-hierarchical-emax_files/libs/quarto-html/tabsets/tabsets.js" type="module"></script> <script src="2025-09-24-dose-finding-hierarchical-emax_files/libs/quarto-html/popper.min.js"></script> <script src="2025-09-24-dose-finding-hierarchical-emax_files/libs/quarto-html/tippy.umd.min.js"></script> <script src="2025-09-24-dose-finding-hierarchical-emax_files/libs/quarto-html/anchor.min.js"></script> <link href="2025-09-24-dose-finding-hierarchical-emax_files/libs/quarto-html/tippy.css" rel="stylesheet"> <link href="2025-09-24-dose-finding-hierarchical-emax_files/libs/quarto-html/quarto-syntax-highlighting-15634bcf2e68342d4ad2dfa704d543f6.css" rel="stylesheet" id="quarto-text-highlighting-styles"> <script src="2025-09-24-dose-finding-hierarchical-emax_files/libs/bootstrap/bootstrap.min.js"></script> <link href="2025-09-24-dose-finding-hierarchical-emax_files/libs/bootstrap/bootstrap-icons.css" rel="stylesheet"> <link href="2025-09-24-dose-finding-hierarchical-emax_files/libs/bootstrap/bootstrap-efbb37e9afcc02144ebbc7afd12a776f.min.css" rel="stylesheet" append-hash="true" id="quarto-bootstrap" data-mode="light"> <script src="https://cdnjs.cloudflare.com/polyfill/v3/polyfill.min.js?features=es6"></script> <script defer="" src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-chtml-full.js" type="text/javascript"></script> <script type="text/javascript">
const typesetMath = (el) => {
  if (window.MathJax) {
    // MathJax Typeset
    window.MathJax.typeset([el]);
  } else if (window.katex) {
    // KaTeX Render
    var mathElements = el.getElementsByClassName("math");
    var macros = [];
    for (var i = 0; i < mathElements.length; i++) {
      var texText = mathElements[i].firstChild;
      if (mathElements[i].tagName == "SPAN" && texText && texText.data) {
        window.katex.render(texText.data, mathElements[i], {
          displayMode: mathElements[i].classList.contains('display'),
          throwOnError: false,
          macros: macros,
          fleqn: false
        });
      }
    }
  }
}
window.Quarto = {
  typesetMath
};
</script> </head> <body class="fullcontent quarto-light"> <div id="quarto-content" class="page-columns page-rows-contents page-layout-article"> <main class="content" id="quarto-document-content"> <header id="title-block-header" class="quarto-title-block default"> <div class="quarto-title"> <h1 class="title">Dose-finding with a Bayesian hierarchical EMAX model</h1> </div> <div> <div class="description"> Extending a Bayesian hierarchical EMAX model with expert-informed priors for a small dose-finding trial in type 1 diabetes, and testing it by simulation against independent pairwise comparisons. </div> </div> <div class="quarto-title-meta"> <div> <div class="quarto-title-meta-heading">Published</div> <div class="quarto-title-meta-contents"> <p class="date">September 24, 2025</p> </div> </div> </div> </header> <p>Early-phase dose-finding trials are chronically short of data. <strong>ASITI-201-T1D</strong> is a planned randomised dose-finding trial of an islet antigen-specific immunotherapy for type 1 diabetes, comparing placebo against three subcutaneous doses. Only <strong>18 participants</strong> are randomised, in a 6:4:4:4 ratio to placebo and the 0.1, 0.3 and 1 mL doses. With four arms and so few observations, the conventional analysis - a set of <strong>independent pairwise comparisons (IPC)</strong> of each active dose against control - throws away two things we usually believe: that response varies <em>smoothly</em> with dose, and that we already hold <em>prior knowledge</em> from earlier trials.</p> <p>This post walks through a <strong>Bayesian hierarchical EMAX model (BH-EMAX)</strong> that puts both of those beliefs back in, and how it fared against IPC in simulation. The work was presented at the ACTA 2024 Clinical Trials and Registries Symposium; the model follows <a href="https://doi.org/10.1002/sim.8167">Gajewski et al.&nbsp;(2019)</a>, extended to let expert priors on the response probabilities enter cleanly.</p> <section id="the-outcome-model" class="level2"> <h2 class="anchored" data-anchor-id="the-outcome-model">The outcome model</h2> <p>For each dose <span class="math inline">\(d\)</span> we observe a binomial count of favourable outcomes,</p> <p><span class="math display">\[ y_d \sim \operatorname{Binomial}(n_d,\, p_d), \qquad p_d = \operatorname{logit}^{-1}(\theta_d). \]</span></p> <p>Everything happens on the logit scale <span class="math inline">\(\theta_d\)</span>. The control arm (<span class="math inline">\(d = 1\)</span>) is modelled on its own with a deliberately vague prior, <span class="math inline">\(\theta_1 \sim \operatorname{Normal}(0, 10^2)\)</span>, so placebo response is estimated from placebo data alone.</p> <p>The two models differ only in how they treat the <em>active</em> doses.</p> <p><strong>Independent pairwise comparisons (IPC).</strong> Each active dose gets its own logit, drawn from an independent, weakly informative prior. There is no link between doses: learning that 0.3 mL works tells the model nothing about 1 mL. This is robust but wasteful when the sample is tiny.</p> <p><strong>Hierarchical EMAX (BH-EMAX).</strong> The active doses share a smooth dose-response curve. Writing <span class="math inline">\(\nu_d = \sqrt{\text{dose}_d}\)</span> for the <em>effective dose strength</em>,</p> <p><span class="math display">\[ \theta_d = \phi_1 + \frac{\phi_2\,\nu_d}{\nu_d + \phi_3} + \psi_d, \qquad d \in \{2,\dots,D\}. \]</span></p> <p>Each parameter has a clear reading:</p> <ul> <li><span class="math inline">\(\phi_1\)</span> is the <strong>floor</strong> - the logit response as dose strength goes to zero. Its prior <span class="math inline">\(\phi_1 \sim \operatorname{Normal}(-3.944, 1)\)</span> sits at <span class="math inline">\(\operatorname{logit}^{-1}(-3.944) \approx 0.02\)</span>, i.e.&nbsp;a ~2% background response.</li> <li><span class="math inline">\(\phi_2\)</span> is the <strong>maximum effect</strong> above the floor, <span class="math inline">\(\phi_2 \sim \operatorname{Normal}(0, 10^2)\)</span>.</li> <li><span class="math inline">\(\phi_3\)</span> is the <strong>ED50</strong>, the dose strength delivering half of that maximum effect, with a positively truncated prior <span class="math inline">\(\phi_3 \sim \operatorname{Normal}(3, 10^2)\,\mathbb{1}[0,500]\)</span>.</li> <li><span class="math inline">\(\psi_d\)</span> is a <strong>hierarchical deviation</strong> that lets each dose depart from the smooth curve, <span class="math inline">\(\psi_d \sim \operatorname{Normal}(0, \phi_4)\)</span> with <span class="math inline">\(\phi_4 \sim \operatorname{InvGamma}(2, 2)\)</span>. Shrinking these deviations toward zero is what lets the model bend to a <strong>non-monotonic</strong> dose-response without abandoning the EMAX shape entirely.</li> </ul> <p>In Stan, the transformed parameters and model block are compact:</p> <div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb1"><pre class="sourceCode stan code-with-copy"><code class="sourceCode stan"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true" tabindex="-1"></a><span class="kw">transformed parameters</span> {</span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true" tabindex="-1"></a>  <span class="dt">real</span> theta[D];</span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true" tabindex="-1"></a>  <span class="dt">real</span> P[D];</span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true" tabindex="-1"></a>  theta[<span class="dv">1</span>] = theta_1;                 <span class="co">// control dose, modelled separately</span></span>
<span id="cb1-5"><a href="#cb1-5" aria-hidden="true" tabindex="-1"></a>  P[<span class="dv">1</span>] = inv_logit(theta_1);</span>
<span id="cb1-6"><a href="#cb1-6" aria-hidden="true" tabindex="-1"></a>  <span class="cf">for</span> (d <span class="cf">in</span> <span class="dv">2</span>:D) {</span>
<span id="cb1-7"><a href="#cb1-7" aria-hidden="true" tabindex="-1"></a>    theta[d] = phi[<span class="dv">1</span>] + phi[<span class="dv">2</span>] * nu[d] / (nu[d] + phi[<span class="dv">3</span>]) + psi[d];</span>
<span id="cb1-8"><a href="#cb1-8" aria-hidden="true" tabindex="-1"></a>    P[d] = inv_logit(theta[d]);</span>
<span id="cb1-9"><a href="#cb1-9" aria-hidden="true" tabindex="-1"></a>  }</span>
<span id="cb1-10"><a href="#cb1-10" aria-hidden="true" tabindex="-1"></a>}</span>
<span id="cb1-11"><a href="#cb1-11" aria-hidden="true" tabindex="-1"></a><span class="kw">model</span> {</span>
<span id="cb1-12"><a href="#cb1-12" aria-hidden="true" tabindex="-1"></a>  <span class="cf">for</span> (d <span class="cf">in</span> <span class="dv">1</span>:D)</span>
<span id="cb1-13"><a href="#cb1-13" aria-hidden="true" tabindex="-1"></a>    <span class="kw">target +=</span> beta_lpdf(P[d] | alpha_pseudo[d], beta_pseudo[d]);  <span class="co">// expert prior</span></span>
<span id="cb1-14"><a href="#cb1-14" aria-hidden="true" tabindex="-1"></a>  phi[<span class="dv">1</span>] ~ normal(-<span class="fl">3.944</span>, <span class="dv">1</span>);         <span class="co">// logit floor (~2% response)</span></span>
<span id="cb1-15"><a href="#cb1-15" aria-hidden="true" tabindex="-1"></a>  phi[<span class="dv">2</span>] ~ normal(<span class="dv">0</span>, <span class="dv">10</span>);             <span class="co">// maximum effect above the floor</span></span>
<span id="cb1-16"><a href="#cb1-16" aria-hidden="true" tabindex="-1"></a>  phi[<span class="dv">3</span>] ~ normal(<span class="dv">3</span>, <span class="dv">10</span>) <span class="kw">T</span>[<span class="dv">0</span>, <span class="dv">500</span>];   <span class="co">// ED50</span></span>
<span id="cb1-17"><a href="#cb1-17" aria-hidden="true" tabindex="-1"></a>  phi4 ~ inv_gamma(<span class="dv">2</span>, <span class="dv">2</span>);             <span class="co">// hierarchical SD</span></span>
<span id="cb1-18"><a href="#cb1-18" aria-hidden="true" tabindex="-1"></a>  psi  ~ normal(<span class="dv">0</span>, phi4);             <span class="co">// dose-specific deviations</span></span>
<span id="cb1-19"><a href="#cb1-19" aria-hidden="true" tabindex="-1"></a>  y ~ binomial(n, P);                 <span class="co">// likelihood</span></span>
<span id="cb1-20"><a href="#cb1-20" aria-hidden="true" tabindex="-1"></a>}</span></code></pre></div><button title="Copy to Clipboard" class="code-copy-button"><i class="bi"></i></button></div> </section> <section id="borrowing-the-clinicians-expectation" class="level2"> <h2 class="anchored" data-anchor-id="borrowing-the-clinicians-expectation">Borrowing the clinician’s expectation</h2> <p>The line that does the extra work is the <code>beta_lpdf</code> term. Rather than only regularising the abstract EMAX parameters, we place a prior <strong>directly on each response probability</strong> <span class="math inline">\(p_d\)</span>:</p> <p><span class="math display">\[ p_d \sim \operatorname{Beta}(\alpha_d, \beta_d). \]</span></p> <p>This is convenient because clinicians reason in terms of response <em>rates</em>, not logit intercepts. Given an expert’s expected proportions <span class="math inline">\(p^\ast_d\)</span>, the poster’s recipe turns each into a pseudo-count by rounding it to the trial’s arm size,</p> <p><span class="math display">\[ y_d = \begin{cases} \lfloor n_d p^\ast_d \rfloor, &amp; n_d p^\ast_d - \lfloor n_d p^\ast_d \rfloor &lt; \tfrac12, \\[2pt] \lceil n_d p^\ast_d \rceil, &amp; \text{otherwise,} \end{cases} \qquad p_d \sim \operatorname{Beta}(0.5 + y_d,\ 0.5 + n_d - y_d), \]</span></p> <p>so that the Beta’s <strong>mean is the expert’s expectation</strong> and its <strong>concentration <span class="math inline">\(\alpha_d + \beta_d\)</span> is the strength</strong> - literally how many pseudo-participants the opinion is worth. Calibrating to the <em>overdose</em> expectation (best response at the middle 0.3 mL dose), the priors used were:</p> <table class="caption-top table"> <colgroup> <col style="width: 20%"> <col style="width: 26%"> <col style="width: 26%"> <col style="width: 26%"> </colgroup> <thead> <tr class="header"> <th style="text-align: left;">Dose (mL)</th> <th style="text-align: center;">Expert mean <span class="math inline">\(p^\ast\)</span></th> <th style="text-align: center;">Low prior <span class="math inline">\(\operatorname{Beta}(\alpha, \beta)\)</span></th> <th style="text-align: center;">High prior <span class="math inline">\(\operatorname{Beta}(\alpha, \beta)\)</span></th> </tr> </thead> <tbody> <tr class="odd"> <td style="text-align: left;">0 (placebo)</td> <td style="text-align: center;">0.01</td> <td style="text-align: center;">(1, 99)</td> <td style="text-align: center;">(1, 99)</td> </tr> <tr class="even"> <td style="text-align: left;">0.1</td> <td style="text-align: center;">0.20</td> <td style="text-align: center;">(0.4, 1.6)</td> <td style="text-align: center;">(0.8, 3.2)</td> </tr> <tr class="odd"> <td style="text-align: left;">0.3</td> <td style="text-align: center;">0.60</td> <td style="text-align: center;">(1.2, 0.8)</td> <td style="text-align: center;">(2.4, 1.6)</td> </tr> <tr class="even"> <td style="text-align: left;">1.0</td> <td style="text-align: center;">0.40</td> <td style="text-align: center;">(0.8, 1.2)</td> <td style="text-align: center;">(1.6, 2.4)</td> </tr> </tbody> </table> <p>Both strengths encode the <em>same</em> expectation; they differ only in concentration - “low” is worth about two pseudo-observations per arm, “high” about four (a full arm’s worth). The uninformative baseline is a Jeffreys <span class="math inline">\(\operatorname{Beta}(0.5, 0.5)\)</span> on every dose. In the simulation harness this is just a switch:</p> <div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb2"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true" tabindex="-1"></a><span class="cf">if</span> (PriorStrength <span class="sc">==</span> <span class="st">"high"</span>) {</span>
<span id="cb2-2"><a href="#cb2-2" aria-hidden="true" tabindex="-1"></a>  alpha_pseudo <span class="ot">&lt;-</span> <span class="fu">c</span>(<span class="dv">1</span>, <span class="fl">0.80</span>, <span class="fl">2.4</span>, <span class="fl">1.60</span>)</span>
<span id="cb2-3"><a href="#cb2-3" aria-hidden="true" tabindex="-1"></a>  beta_pseudo  <span class="ot">&lt;-</span> <span class="fu">c</span>(<span class="dv">99</span>, <span class="fl">3.20</span>, <span class="fl">1.6</span>, <span class="fl">2.40</span>)</span>
<span id="cb2-4"><a href="#cb2-4" aria-hidden="true" tabindex="-1"></a>}</span></code></pre></div><button title="Copy to Clipboard" class="code-copy-button"><i class="bi"></i></button></div> </section> <section id="simulation-study" class="level2"> <h2 class="anchored" data-anchor-id="simulation-study">Simulation study</h2> <p>Response data were simulated for <strong>10,000 trials</strong> under two truths:</p> <ul> <li><strong>Monotonic:</strong> <span class="math inline">\(p = (0.05, 0.20, 0.40, 0.60)\)</span> - the highest dose (1 mL) is genuinely best.</li> <li><strong>Overdose / non-monotonic:</strong> <span class="math inline">\(p = (0.05, 0.20, 0.60, 0.40)\)</span> - the middle dose (0.3 mL) is best.</li> </ul> <p>The expert prior was calibrated to the <em>overdose</em> pattern. That sets up the honest test: in the overdose scenario the prior <strong>agrees</strong> with the truth, while in the monotonic scenario the prior actively <strong>disagrees</strong> with it, expecting the wrong dose to win. Each fitted model reports, per simulated trial, which dose carries the highest posterior response probability (via a <code>Max_indicator</code>), and we average that over the 10,000 trials to get the probability of selecting each dose as best.</p> </section> <section id="results" class="level2"> <h2 class="anchored" data-anchor-id="results">Results</h2> <p>The headline is the probability that each dose is picked as the winner (<strong>bold</strong> marks the truly best dose):</p> <table class="caption-top table"> <thead> <tr class="header"> <th style="text-align: left;">Scenario</th> <th style="text-align: left;">Model</th> <th style="text-align: center;">0.1 mL</th> <th style="text-align: center;">0.3 mL</th> <th style="text-align: center;">1 mL</th> </tr> </thead> <tbody> <tr class="odd"> <td style="text-align: left;">Monotonic</td> <td style="text-align: left;">BH-EMAX</td> <td style="text-align: center;">0.10</td> <td style="text-align: center;">0.27</td> <td style="text-align: center;"><strong>0.62</strong></td> </tr> <tr class="even"> <td style="text-align: left;">Monotonic</td> <td style="text-align: left;">IPC</td> <td style="text-align: center;">0.15</td> <td style="text-align: center;">0.31</td> <td style="text-align: center;"><strong>0.54</strong></td> </tr> <tr class="odd"> <td style="text-align: left;">Overdose</td> <td style="text-align: left;">BH-EMAX</td> <td style="text-align: center;">0.10</td> <td style="text-align: center;"><strong>0.58</strong></td> <td style="text-align: center;">0.30</td> </tr> <tr class="even"> <td style="text-align: left;">Overdose</td> <td style="text-align: left;">IPC</td> <td style="text-align: center;">0.15</td> <td style="text-align: center;"><strong>0.54</strong></td> <td style="text-align: center;">0.31</td> </tr> </tbody> </table> <p>So the probability of correctly identifying the best dose was <strong>62% (BH-EMAX) vs 54% (IPC)</strong> under the monotonic truth, and <strong>58% vs 54%</strong> under the non-monotonic truth. BH-EMAX comes out ahead in both - and, encouragingly, it still beats IPC in the monotonic scenario <em>even though its prior was pointing at the wrong dose</em>, because the smooth EMAX curve pulls the estimate back toward a monotone shape that the data support. In the overdose scenario, where prior and truth agree, the borrowed opinion sharpens the middle dose further.</p> <p>The cost of the extra structure showed up as <strong>convergence issues</strong> in the minority of simulated datasets where a small, noisy sample conflicted sharply with the calibrated prior - unsurprising when a four-parameter curve plus a hierarchical variance is asked to fit 18 observations.</p> </section> <section id="takeaways" class="level2"> <h2 class="anchored" data-anchor-id="takeaways">Takeaways</h2> <ul> <li>On a genuinely tiny trial, <strong>borrowing structure across doses and encoding expert opinion buys a few percentage points</strong> of correct dose selection over independent comparisons - modest, but free in the sense that it costs no extra participants.</li> <li>Placing the prior <strong>on the response probabilities</strong> rather than the EMAX coefficients keeps elicitation in the clinician’s natural units and makes the <em>strength</em> of the opinion an explicit, tunable dial.</li> <li>Neither model is decisive at <span class="math inline">\(n = 18\)</span>: both sit around 55-62% correct selection, a blunt reminder that no amount of modelling manufactures information the data never contained. The hierarchical EMAX helps at the margin and degrades gracefully when its prior is wrong, which is about the most one can ask of a small dose-finding design.</li> </ul> </section> </main> <script id="quarto-html-after-body" type="application/javascript">
  window.document.addEventListener("DOMContentLoaded", function (event) {
    const icon = "";
    const anchorJS = new window.AnchorJS();
    anchorJS.options = {
      placement: 'right',
      icon: icon
    };
    anchorJS.add('.anchored');
    const isCodeAnnotation = (el) => {
      for (const clz of el.classList) {
        if (clz.startsWith('code-annotation-')) {                     
          return true;
        }
      }
      return false;
    }
    const onCopySuccess = function(e) {
      // button target
      const button = e.trigger;
      // don't keep focus
      button.blur();
      // flash "checked"
      button.classList.add('code-copy-button-checked');
      var currentTitle = button.getAttribute("title");
      button.setAttribute("title", "Copied!");
      let tooltip;
      if (window.bootstrap) {
        button.setAttribute("data-bs-toggle", "tooltip");
        button.setAttribute("data-bs-placement", "left");
        button.setAttribute("data-bs-title", "Copied!");
        tooltip = new bootstrap.Tooltip(button, 
          { trigger: "manual", 
            customClass: "code-copy-button-tooltip",
            offset: [0, -8]});
        tooltip.show();    
      }
      setTimeout(function() {
        if (tooltip) {
          tooltip.hide();
          button.removeAttribute("data-bs-title");
          button.removeAttribute("data-bs-toggle");
          button.removeAttribute("data-bs-placement");
        }
        button.setAttribute("title", currentTitle);
        button.classList.remove('code-copy-button-checked');
      }, 1000);
      // clear code selection
      e.clearSelection();
    }
    const getTextToCopy = function(trigger) {
      const outerScaffold = trigger.parentElement.cloneNode(true);
      const codeEl = outerScaffold.querySelector('code');
      for (const childEl of codeEl.children) {
        if (isCodeAnnotation(childEl)) {
          childEl.remove();
        }
      }
      return codeEl.innerText;
    }
    const clipboard = new window.ClipboardJS('.code-copy-button:not([data-in-quarto-modal])', {
      text: getTextToCopy
    });
    clipboard.on('success', onCopySuccess);
    if (window.document.getElementById('quarto-embedded-source-code-modal')) {
      const clipboardModal = new window.ClipboardJS('.code-copy-button[data-in-quarto-modal]', {
        text: getTextToCopy,
        container: window.document.getElementById('quarto-embedded-source-code-modal')
      });
      clipboardModal.on('success', onCopySuccess);
    }
      var localhostRegex = new RegExp(/^(?:http|https):\/\/localhost\:?[0-9]*\//);
      var mailtoRegex = new RegExp(/^mailto:/);
        var filterRegex = new RegExp('/' + window.location.host + '/');
      var isInternal = (href) => {
          return filterRegex.test(href) || localhostRegex.test(href) || mailtoRegex.test(href);
      }
      // Inspect non-navigation links and adorn them if external
     var links = window.document.querySelectorAll('a[href]:not(.nav-link):not(.navbar-brand):not(.toc-action):not(.sidebar-link):not(.sidebar-item-toggle):not(.pagination-link):not(.no-external):not([aria-hidden]):not(.dropdown-item):not(.quarto-navigation-tool):not(.about-link)');
      for (var i=0; i<links.length; i++) {
        const link = links[i];
        if (!isInternal(link.href)) {
          // undo the damage that might have been done by quarto-nav.js in the case of
          // links that we want to consider external
          if (link.dataset.originalHref !== undefined) {
            link.href = link.dataset.originalHref;
          }
        }
      }
    function tippyHover(el, contentFn, onTriggerFn, onUntriggerFn) {
      const config = {
        allowHTML: true,
        maxWidth: 500,
        delay: 100,
        arrow: false,
        appendTo: function(el) {
            return el.parentElement;
        },
        interactive: true,
        interactiveBorder: 10,
        theme: 'quarto',
        placement: 'bottom-start',
      };
      if (contentFn) {
        config.content = contentFn;
      }
      if (onTriggerFn) {
        config.onTrigger = onTriggerFn;
      }
      if (onUntriggerFn) {
        config.onUntrigger = onUntriggerFn;
      }
      window.tippy(el, config); 
    }
    const noterefs = window.document.querySelectorAll('a[role="doc-noteref"]');
    for (var i=0; i<noterefs.length; i++) {
      const ref = noterefs[i];
      tippyHover(ref, function() {
        // use id or data attribute instead here
        let href = ref.getAttribute('data-footnote-href') || ref.getAttribute('href');
        try { href = new URL(href).hash; } catch {}
        const id = href.replace(/^#\/?/, "");
        const note = window.document.getElementById(id);
        if (note) {
          return note.innerHTML;
        } else {
          return "";
        }
      });
    }
    const xrefs = window.document.querySelectorAll('a.quarto-xref');
    const processXRef = (id, note) => {
      // Strip column container classes
      const stripColumnClz = (el) => {
        el.classList.remove("page-full", "page-columns");
        if (el.children) {
          for (const child of el.children) {
            stripColumnClz(child);
          }
        }
      }
      stripColumnClz(note)
      if (id === null || id.startsWith('sec-')) {
        // Special case sections, only their first couple elements
        const container = document.createElement("div");
        if (note.children && note.children.length > 2) {
          container.appendChild(note.children[0].cloneNode(true));
          for (let i = 1; i < note.children.length; i++) {
            const child = note.children[i];
            if (child.tagName === "P" && child.innerText === "") {
              continue;
            } else {
              container.appendChild(child.cloneNode(true));
              break;
            }
          }
          if (window.Quarto?.typesetMath) {
            window.Quarto.typesetMath(container);
          }
          return container.innerHTML
        } else {
          if (window.Quarto?.typesetMath) {
            window.Quarto.typesetMath(note);
          }
          return note.innerHTML;
        }
      } else {
        // Remove any anchor links if they are present
        const anchorLink = note.querySelector('a.anchorjs-link');
        if (anchorLink) {
          anchorLink.remove();
        }
        if (window.Quarto?.typesetMath) {
          window.Quarto.typesetMath(note);
        }
        if (note.classList.contains("callout")) {
          return note.outerHTML;
        } else {
          return note.innerHTML;
        }
      }
    }
    for (var i=0; i<xrefs.length; i++) {
      const xref = xrefs[i];
      tippyHover(xref, undefined, function(instance) {
        instance.disable();
        let url = xref.getAttribute('href');
        let hash = undefined; 
        if (url.startsWith('#')) {
          hash = url;
        } else {
          try { hash = new URL(url).hash; } catch {}
        }
        if (hash) {
          const id = hash.replace(/^#\/?/, "");
          const note = window.document.getElementById(id);
          if (note !== null) {
            try {
              const html = processXRef(id, note.cloneNode(true));
              instance.setContent(html);
            } finally {
              instance.enable();
              instance.show();
            }
          } else {
            // See if we can fetch this
            fetch(url.split('#')[0])
            .then(res => res.text())
            .then(html => {
              const parser = new DOMParser();
              const htmlDoc = parser.parseFromString(html, "text/html");
              const note = htmlDoc.getElementById(id);
              if (note !== null) {
                const html = processXRef(id, note);
                instance.setContent(html);
              } 
            }).finally(() => {
              instance.enable();
              instance.show();
            });
          }
        } else {
          // See if we can fetch a full url (with no hash to target)
          // This is a special case and we should probably do some content thinning / targeting
          fetch(url)
          .then(res => res.text())
          .then(html => {
            const parser = new DOMParser();
            const htmlDoc = parser.parseFromString(html, "text/html");
            const note = htmlDoc.querySelector('main.content');
            if (note !== null) {
              // This should only happen for chapter cross references
              // (since there is no id in the URL)
              // remove the first header
              if (note.children.length > 0 && note.children[0].tagName === "HEADER") {
                note.children[0].remove();
              }
              const html = processXRef(null, note);
              instance.setContent(html);
            } 
          }).finally(() => {
            instance.enable();
            instance.show();
          });
        }
      }, function(instance) {
      });
    }
        let selectedAnnoteEl;
        const selectorForAnnotation = ( cell, annotation) => {
          let cellAttr = 'data-code-cell="' + cell + '"';
          let lineAttr = 'data-code-annotation="' +  annotation + '"';
          const selector = 'span[' + cellAttr + '][' + lineAttr + ']';
          return selector;
        }
        const selectCodeLines = (annoteEl) => {
          const doc = window.document;
          const targetCell = annoteEl.getAttribute("data-target-cell");
          const targetAnnotation = annoteEl.getAttribute("data-target-annotation");
          const annoteSpan = window.document.querySelector(selectorForAnnotation(targetCell, targetAnnotation));
          const lines = annoteSpan.getAttribute("data-code-lines").split(",");
          const lineIds = lines.map((line) => {
            return targetCell + "-" + line;
          })
          let top = null;
          let height = null;
          let parent = null;
          if (lineIds.length > 0) {
              //compute the position of the single el (top and bottom and make a div)
              const el = window.document.getElementById(lineIds[0]);
              top = el.offsetTop;
              height = el.offsetHeight;
              parent = el.parentElement.parentElement;
            if (lineIds.length > 1) {
              const lastEl = window.document.getElementById(lineIds[lineIds.length - 1]);
              const bottom = lastEl.offsetTop + lastEl.offsetHeight;
              height = bottom - top;
            }
            if (top !== null && height !== null && parent !== null) {
              // cook up a div (if necessary) and position it 
              let div = window.document.getElementById("code-annotation-line-highlight");
              if (div === null) {
                div = window.document.createElement("div");
                div.setAttribute("id", "code-annotation-line-highlight");
                div.style.position = 'absolute';
                parent.appendChild(div);
              }
              div.style.top = top - 2 + "px";
              div.style.height = height + 4 + "px";
              div.style.left = 0;
              let gutterDiv = window.document.getElementById("code-annotation-line-highlight-gutter");
              if (gutterDiv === null) {
                gutterDiv = window.document.createElement("div");
                gutterDiv.setAttribute("id", "code-annotation-line-highlight-gutter");
                gutterDiv.style.position = 'absolute';
                const codeCell = window.document.getElementById(targetCell);
                const gutter = codeCell.querySelector('.code-annotation-gutter');
                gutter.appendChild(gutterDiv);
              }
              gutterDiv.style.top = top - 2 + "px";
              gutterDiv.style.height = height + 4 + "px";
            }
            selectedAnnoteEl = annoteEl;
          }
        };
        const unselectCodeLines = () => {
          const elementsIds = ["code-annotation-line-highlight", "code-annotation-line-highlight-gutter"];
          elementsIds.forEach((elId) => {
            const div = window.document.getElementById(elId);
            if (div) {
              div.remove();
            }
          });
          selectedAnnoteEl = undefined;
        };
          // Handle positioning of the toggle
      window.addEventListener(
        "resize",
        throttle(() => {
          elRect = undefined;
          if (selectedAnnoteEl) {
            selectCodeLines(selectedAnnoteEl);
          }
        }, 10)
      );
      function throttle(fn, ms) {
      let throttle = false;
      let timer;
        return (...args) => {
          if(!throttle) { // first call gets through
              fn.apply(this, args);
              throttle = true;
          } else { // all the others get throttled
              if(timer) clearTimeout(timer); // cancel #2
              timer = setTimeout(() => {
                fn.apply(this, args);
                timer = throttle = false;
              }, ms);
          }
        };
      }
        // Attach click handler to the DT
        const annoteDls = window.document.querySelectorAll('dt[data-target-cell]');
        for (const annoteDlNode of annoteDls) {
          annoteDlNode.addEventListener('click', (event) => {
            const clickedEl = event.target;
            if (clickedEl !== selectedAnnoteEl) {
              unselectCodeLines();
              const activeEl = window.document.querySelector('dt[data-target-cell].code-annotation-active');
              if (activeEl) {
                activeEl.classList.remove('code-annotation-active');
              }
              selectCodeLines(clickedEl);
              clickedEl.classList.add('code-annotation-active');
            } else {
              // Unselect the line
              unselectCodeLines();
              clickedEl.classList.remove('code-annotation-active');
            }
          });
        }
    const findCites = (el) => {
      const parentEl = el.parentElement;
      if (parentEl) {
        const cites = parentEl.dataset.cites;
        if (cites) {
          return {
            el,
            cites: cites.split(' ')
          };
        } else {
          return findCites(el.parentElement)
        }
      } else {
        return undefined;
      }
    };
    var bibliorefs = window.document.querySelectorAll('a[role="doc-biblioref"]');
    for (var i=0; i<bibliorefs.length; i++) {
      const ref = bibliorefs[i];
      const citeInfo = findCites(ref);
      if (citeInfo) {
        tippyHover(citeInfo.el, function() {
          var popup = window.document.createElement('div');
          citeInfo.cites.forEach(function(cite) {
            var citeDiv = window.document.createElement('div');
            citeDiv.classList.add('hanging-indent');
            citeDiv.classList.add('csl-entry');
            var biblioDiv = window.document.getElementById('ref-' + cite);
            if (biblioDiv) {
              citeDiv.innerHTML = biblioDiv.innerHTML;
            }
            popup.appendChild(citeDiv);
          });
          return popup.innerHTML;
        });
      }
    }
  });
  </script> </div> </body></html>]]></content><author><name></name></author><summary type="html"><![CDATA[]]></summary></entry><entry><title type="html">Frequentist models for non-informative Bayesian designs</title><link href="https://pmaher1.github.io/blog/2025/frequentist-models-non-informative-bayesian/" rel="alternate" type="text/html" title="Frequentist models for non-informative Bayesian designs"/><published>2025-09-23T00:00:00+00:00</published><updated>2025-09-23T00:00:00+00:00</updated><id>https://pmaher1.github.io/blog/2025/frequentist-models-non-informative-bayesian</id><content type="html" xml:base="https://pmaher1.github.io/blog/2025/frequentist-models-non-informative-bayesian/"><![CDATA[<p>WIP</p>]]></content><author><name></name></author><summary type="html"><![CDATA[WIP]]></summary></entry><entry><title type="html">Gaussian Processes for semiparametric accelerated-failure time joint modelling</title><link href="https://pmaher1.github.io/blog/2025/gaussian-processes-semiparametric-aft-joint-modelling/" rel="alternate" type="text/html" title="Gaussian Processes for semiparametric accelerated-failure time joint modelling"/><published>2025-09-22T00:00:00+00:00</published><updated>2025-09-22T00:00:00+00:00</updated><id>https://pmaher1.github.io/blog/2025/gaussian-processes-semiparametric-aft-joint-modelling</id><content type="html" xml:base="https://pmaher1.github.io/blog/2025/gaussian-processes-semiparametric-aft-joint-modelling/"><![CDATA[<p>WIP</p>]]></content><author><name></name></author><summary type="html"><![CDATA[WIP]]></summary></entry><entry><title type="html">Quick Estimates in Missing Data Structures</title><link href="https://pmaher1.github.io/blog/2025/quick-estimates-missing-data/" rel="alternate" type="text/html" title="Quick Estimates in Missing Data Structures"/><published>2025-09-21T00:00:00+00:00</published><updated>2025-09-21T00:00:00+00:00</updated><id>https://pmaher1.github.io/blog/2025/quick-estimates-missing-data</id><content type="html" xml:base="https://pmaher1.github.io/blog/2025/quick-estimates-missing-data/"><![CDATA[<p>WIP</p>]]></content><author><name></name></author><summary type="html"><![CDATA[WIP]]></summary></entry></feed>